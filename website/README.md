# ilyStream Website

Astro landing site for the ilyStream preview release.

## Commands

```sh
npm install
npm run dev
npm run build
npm run preview
```

The production build outputs to `website/dist/`.
