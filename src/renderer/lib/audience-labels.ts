import type { Platform } from '../../../main/platforms/types'

export function subscriptionMetricLabel(platform: Platform | string) { return 'Subscribers' }
export function subscriptionMetricTitle(platform: Platform | string) { return 'Subscribers' }
